server:
  port: ${SERVER_PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI:https://github.com/Hoteleria-Luxe-Stay/config-repo.git}
          default-label: main
          clone-on-start: true
