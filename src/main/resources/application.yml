server:
  port: ${SERVER_PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Hoteleria-Luxe-Stay/config-repo.git
          default-label: main
          clone-on-start: true
